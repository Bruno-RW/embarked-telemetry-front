import { LocationType } from "@shared/types/data/location";
import { AddressType } from "@shared/types/data/address";

export type TelemetryType = {
    location: LocationType;
    address: AddressType;
}